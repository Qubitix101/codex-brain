# Step 3 - Context Health Check

Run `scripts/context-health.mjs` when available.

Verify:

- state exists
- project context exists
- active context exists
- progress memory exists
- session record exists
- next action is populated

If health fails, fix the missing context before ending the session.

