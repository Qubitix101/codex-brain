# Step 2 - Compute Next Action

Use the active phase and gate to determine:

- missing artifacts
- approval required
- allowed actions
- blocked actions
- recommended workflow

If `scripts/next-action.mjs` is available, run it.

If not, apply `frameworks/context-routing-and-help.md` manually.

Output one next action, not a menu of every possible action.

